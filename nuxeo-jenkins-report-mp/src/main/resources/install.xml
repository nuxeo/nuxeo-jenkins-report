<install>
  <update file="${package.root}/install/bundles" todir="${env.bundles}" />
  <delete file="${env.lib}/mvel*" />
  <update file="${package.root}/install/libs" todir="${env.lib}" />
</install>
